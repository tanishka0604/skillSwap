# SkillSwap database configuration
# ---------------------------------
# Replace db.user and db.password with YOUR OWN MySQL credentials.
# This file lives in WEB-INF/classes so it is never served to a browser
# (everything under WEB-INF is private to the server), and it's kept
# separate from the .java files so your credentials aren't hardcoded
# inside compiled code or committed to version control by accident.
#
# If you use Git, add this exact path to your .gitignore:
#   src/main/webapp/WEB-INF/classes/db.properties

db.url=jdbc:mysql://localhost:3306/skillswap?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=Tanishka@2004

